{
  "name": "@springmatter/components",
  "version": "4.5.7",
  "main": "dist/index.js",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "./build.sh",
    "pub": "yarn run build && yarn publish",
    "pub-patch": "yarn version patch && yarn run pub",
    "pub-minor": "yarn version minor && yarn run pub"
  },
  "dependencies": {
    "feather-icons": "^4.24.1",
    "fuse.js": "^3.4.5"
  },
  "devDependencies": {
    "tailwindcss": "^1.1.3"
  }
}
